# Быстрый старт: Прокси-менеджер для Bybit Parser

## 🚀 Что это?

Профессиональная система управления прокси-серверами для обхода ограничений Bybit API.

## ⚡ Быстрая настройка

### 1. Добавить прокси

Откройте `config.py` и добавьте свои прокси:

```python
PROXIES = [
    "IP:PORT:LOGIN:PASSWORD",
    "IP:PORT:LOGIN:PASSWORD",
    "IP:PORT:LOGIN:PASSWORD"
]
```

### 2. Настроить параметры

```python
REQUEST_TIMEOUT = 10           # Таймаут (секунды)
MAX_RETRIES = 3                # Попытки на запрос
PROXY_USE_PROBABILITY = 0.7    # 70% через прокси
```

### 3. Деплой

```bash
sync_backend()
```

Готово! Система работает автоматически.

## 📊 Мониторинг

### В ответе API

```json
{
  "offers": [...],
  "proxy_stats": {
    "total_requests": 10,
    "successful_requests": 9,
    "success_rate": 90.0,
    "proxy_usage_rate": 70.0
  }
}
```

### В логах

```
[ProxyManager] PROXY: 157.22.11.191:63616 | STATUS: SUCCESS | CODE: 200 | TIME: 1.2s
```

### В UI

Индикатор 🔒 показывает success rate прокси.

## 🔧 Типичные настройки

### Редкие запросы (экономия прокси)
```python
PROXY_USE_PROBABILITY = 0.3
MAX_RETRIES = 2
```

### Частые запросы (защита от блокировок)
```python
PROXY_USE_PROBABILITY = 0.9
MAX_RETRIES = 5
```

### Максимальная надёжность
```python
PROXY_USE_PROBABILITY = 1.0
MAX_RETRIES = 5
REQUEST_TIMEOUT = 20
```

## ❗ Troubleshooting

| Проблема | Решение |
|----------|---------|
| Прокси не работают | Проверьте формат IP:PORT:LOGIN:PASSWORD |
| Медленно | Увеличьте REQUEST_TIMEOUT |
| 429 ошибки | Добавьте больше прокси или увеличьте задержки |

## 📈 Что делает система

✅ Автоматически выбирает прокси  
✅ Переключается при ошибках  
✅ Возвращается к прямому подключению если нужно  
✅ Логирует все операции  
✅ Собирает статистику  

Всё работает "из коробки" — просто добавьте прокси в config.py!

---

Для детальной документации см. [README.md](./README.md)
